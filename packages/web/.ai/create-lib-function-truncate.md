In `src/lib/truncate.ts` create a function that takes whole address like
`0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` or any hex string starting from `0x` and returns its
truncated like `0x8335...2913`. The function should accept parameter for how many digits are
visible. Write also a test suite for that function.

Follow rules in files:

<rules>
`.cursor/rules/testing.mdc`
</rules>
