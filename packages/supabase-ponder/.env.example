# Supabase Ponder Database Client Configuration
# ==============================================
# This should point to the same Supabase PostgreSQL database
# that Ponder uses for indexing

# Supabase PostgreSQL database (run `pnpm db:start` first)
PONDER_DB_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54332/postgres
